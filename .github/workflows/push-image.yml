name: Docker Image CI for GHCR
on:
  push
jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and push the image
        run : |
          docker login --username harshpanchal18 --password ${{ secrets.GH_PAT }} ghcr.io
          docker build . --tag ghcr.io/harshpanchal18/ecommerce-osms:latest
          docker push ghcr.io/harshpanchal18/ecommerce-osms:latest
  # build-and-deploy:
  #   runs-on: self-hosted
  #   steps:
  #     - name: deploy on your cluster
  #       run: |
  #         kubectl --kubeconfig /Users/harsh/.kube/config apply - f /Users/harsh/actions-runner/_work/ecommerce-osms/ecommerce-osms/deployment.yml